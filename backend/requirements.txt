fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.2
psycopg[binary]==3.2.1
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
email-validator==2.2.0
httpx==0.27.2
python-dateutil==2.9.0.post0
# Banco de fusos horários do IANA como pacote Python — a imagem base do
# Docker (python:3.12-slim) não traz /usr/share/zoneinfo, então sem isto
# zoneinfo.ZoneInfo("America/Sao_Paulo") falharia em produção mesmo
# funcionando local (onde o SO costuma ter o pacote tzdata instalado).
tzdata==2024.2
